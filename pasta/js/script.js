let  notas=0;
let  soma=0;
var  i=0; 
while(notas != 'fim'){
	notas = prompt('Qual sua nota?'."" );
	if(notas == 'fim')  { 
		notas += number(n);
		i++
	}
		let media=notas/i;
		alert('notas' media);
}